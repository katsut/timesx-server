{
    "version": "2.0.0",
    "tasks": [
      {
        "type": "shell",
        "command": "poetry install",
        "label": "poetry: install",
        "options": {
          "cwd": "${workspaceFolder}"
        },
        "runOptions": { "runOn": "folderOpen" }
      }
    ]
  }
  